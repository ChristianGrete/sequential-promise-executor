import isThenable from './isThenable'

describe('isThenable($value)', () => {
  // TODO: Add actual tests - this is just to pass
  it('is a function', () => {
    expect(typeof isThenable).toBe('function')
  })
})
